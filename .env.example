# Django
SECRET_KEY=change-me
ALLOWED_HOSTS=api.example.com
CORS_ALLOWED_ORIGINS=https://app.example.com
# DATABASE_URL=postgres://user:pass@host:5432/activplug

# Settings module: manage.py defaults to campus.settings.development,
# wsgi/asgi to campus.settings.production. Override if needed:
# DJANGO_SETTINGS_MODULE=campus.settings.production

# Google Sign-In (the only public signup path).
# Google Cloud Console -> APIs & Services -> Credentials -> OAuth client ID
# (Web application). Add your frontend origins (http://localhost:3000 and the
# production domain) as authorized JavaScript origins. The same client id
# goes in the frontend's NEXT_PUBLIC_GOOGLE_CLIENT_ID.
